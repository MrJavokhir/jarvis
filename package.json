{
  "name": "jarvis-reminder-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Ovozli xabar orqali eslatma yaratadigan Telegram bot + Mini App kalendar",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:web": "npm --prefix webapp run dev",
    "build": "npm run build:server && npm run build:web",
    "build:server": "tsc -p tsconfig.json",
    "build:web": "npm --prefix webapp ci --no-audit --no-fund && npm --prefix webapp run build",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit && npm --prefix webapp run typecheck",
    "test": "npm run build:server && node tests/smoke.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "@fastify/static": "^10.1.3",
    "better-sqlite3": "^13.0.3",
    "dotenv": "^17.2.3",
    "fastify": "^5.12.3",
    "grammy": "^1.46.0",
    "luxon": "^3.7.2",
    "openai": "^7.12.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/luxon": "^3.7.1",
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
